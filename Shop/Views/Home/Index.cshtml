@model HomeDTO

<h3>Top selling cars</h3>
<div class=" row mt-5 mb-2">
    @{
        foreach (Car car in Model.TopSellingCars)
        {         
               <article>               
                  @Html.Partial("AllCars", car)               
               </article>    
        }
    }
</div>